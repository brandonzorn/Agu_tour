services:
  nginx:
    image: nginx:alpine
    container_name: agu-tour-nginx
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/conf.d:/etc/nginx/conf.d
      - ./app:/usr/share/nginx/html
      - ./logs:/var/log/nginx
      - ./certbot/conf:/etc/letsencrypt
      - ./certbot/www:/var/www/certbot
    restart: unless-stopped
    depends_on:
      - certbot
    profiles:
      - full

  nginx-http:
    image: nginx:alpine
    container_name: agu-tour-nginx-http
    ports:
      - "80:80"
    volumes:
      - ./nginx/http=only=conf.d:/etc/nginx/conf.d
      - ./certbot/www:/var/www/certbot
    profiles:
      - http-only

  certbot:
    image: certbot/certbot:latest
    container_name: agu-tour-certbot
    volumes:
      - ./certbot/conf:/etc/letsencrypt
      - ./certbot/www:/var/www/certbot
    entrypoint: >
      sh -c "trap exit TERM;
      while :; do
        certbot renew --webroot -w /var/www/certbot --quiet;
        sleep 12h;
      done"
    profiles:
      - full
